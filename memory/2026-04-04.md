# 2026-04-04

## Asset Attention 대규모 리팩토링

### Sharpe 버그 발견 및 수정
- 주인님이 "Sharpe 2.89가 말이 돼?" → sqrt(252) 대신 sqrt(252/REBAL_FREQ) 사용해야 함
- 모든 이전 Sharpe 값이 ~2.24배 과대 계산됨
- EW: 2.89 → 1.39, MinVar w500: 6.26 → 2.87

### Karpathy 수준 리팩토링
- prepare.py에 모든 평가 함수 이동 (sharpe, sortino, MDD, lw_cov, minvar, evaluate_strategy)
- train.py는 모델+학습만 (에이전트가 metrics 재정의 불가)
- guard.py: mission enforcement 추가 (has_attention, preserves_time, regime_signal)
- docs/ 디렉토리로 지침 분리 (philosophy.md, program.md, insights.md, experiments.md)

### 미션 리셋: Attention 기반 레짐 학습
- philosophy.md 재작성: THE MISSION (inviolable) — attention으로 implicit regime learning
- 107개 실험의 근본 실패 원인: "Complexity Must Be Earned" + Sharpe greedy → attention 포기
- program.md: 모든 실험이 반드시 attention-over-time 사용
- 비attention 모델(MLP, MinVar)은 벤치마크일 뿐

### 에이전트 탈선 분석 (Out-of-Loop DreamWalk)
- Exp 1에서 mean(dim=time) MLP가 이김 → 에이전트가 시계열 학습 포기
- insights.md가 잘못된 방향으로 자기강화 루프 형성
- MUST를 쓰고 검증 메커니즘 없으면 에이전트가 무시함

### 주인님과의 모델 설계 대화 (핵심 교훈)
- "피처가 17개야?" → 17 ETF를 17 피처로 취급한 설계 의심
- "시계열 학습이 안 된거 아냐?" → mean(dim=time) 문제 발견
- "다른 논문은 어떻게 했어?" → Portfolio Transformer: sliding window + expanding window
- "5000개로 수만 params?" → 과적합 가능성 지적
- PatchTST channel-independent는 예측용, 배분에서는 자산 동시 입력이 학계 표준
- Loss는 포트폴리오 단위 (자산별 독립 loss 아님)

### Critic Agent 설계
- 페르소나: 10년차 퀀트 CIO ("내 돈 넣을 수 있어?")
- Dream Phase ≠ Critic: Dream은 자기 복기, Critic은 외부 검증
- 8단계 review process: 모델 설명 → 데이터 여정 → 독립 데이터포인트 → 숫자 상식 → 실투자 → attention 분석 → SPY 비교 → 실패 모드
- docs/critic.md에 프롬프트 작성 완료

### v3 모델: Micro-Patch Attention (파일럿)
- 4자산: SPY, TLT, GLD, SHY
- 일간 리밸 → 4636 sliding window samples
- 60일 룩백 → 12패치(5일) → attention over 12 time steps → softmax → 4 weights
- 268 params, expanding window 17년 OOS
- **첫 결과**: attention entropy 모든 연도 동일(2.485) → 레짐 학습 제로, OOS Sharpe 0.88 vs EW 0.89
- attention이 uniform으로 퇴화 — 다음 단계에서 해결 필요

### Cron 업데이트
- autoresearch-respawn watchdog: docs/ 경로 반영
- Self-respawn 규칙 program.md에 추가

### Socratic Self-Check 규칙 추가
- program.md에 모델 설계 전 필수 자문 5가지
- "이 결과가 상식적으로 말이 되나?" "실제로 이렇게 투자하겠나?"
- 주인님 대화 스타일을 에이전트 내부 프로세스로 내재화
